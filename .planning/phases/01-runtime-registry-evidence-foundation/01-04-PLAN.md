---
phase: 01-runtime-registry-evidence-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - omnicontrol/runtime/evidence.py
  - tests/test_runtime_evidence.py
  - tests/test_live_smoke_macos.py
autonomous: true
gap_closure: true
requirements:
  - RTF-04
  - VER-02
must_haves:
  truths:
    - "A generated smoke `result.json` never ships with an empty `artifacts[]` when the bundle itself was materialized on disk."
    - "Finder-open mocked smoke coverage proves the persisted bundle contains concrete artifact references, not only `report_path` and runtime metadata."
    - "Any fallback artifact added for this gap is explicitly labeled as bundle/report evidence, not synthesized profile output."
    - "Legacy consumers still receive the current top-level `report_path` contract unchanged."
  artifacts:
    - path: "omnicontrol/runtime/evidence.py"
      provides: "Backward-compatible artifact normalization that adds a concrete bundle-evidence fallback artifact for materialized smoke bundles with no profile-specific outputs"
      exports: ["write_result_bundle"]
    - path: "tests/test_runtime_evidence.py"
      provides: "Regression coverage for non-empty artifact refs when only the bundle/report exists"
    - path: "tests/test_live_smoke_macos.py"
      provides: "Mocked finder-open smoke regression proving persisted bundle artifacts are populated"
  key_links:
    - from: "omnicontrol/runtime/evidence.py"
      to: "tests/test_runtime_evidence.py"
      via: "fallback artifact normalization assertions"
      pattern: "artifacts"
    - from: "tests/test_live_smoke_macos.py"
      to: "omnicontrol/runtime/evidence.py"
      via: "mocked finder-open bundle persistence assertions"
      pattern: "result\\.json|artifacts"
---

<objective>
Close the verified Phase 1 evidence gap without reopening broader runtime scope.

Purpose: make practical `VER-02` acceptance true for smoke bundles whose payload does not already expose a canonical artifact-bearing key, while preserving the Phase 1 compatibility contract.
Output: one targeted evidence-normalization fix and the narrow tests that prove `finder-open` bundles now persist concrete `artifacts[]` references.
</objective>

<execution_context>
@/Users/daizhaorong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/daizhaorong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-VERIFICATION.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-UAT.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-03-PLAN.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-runtime-registry-evidence-foundation-03-SUMMARY.md
@omnicontrol/runtime/evidence.py
@omnicontrol/runtime/live_smoke.py
@tests/test_runtime_evidence.py
@tests/test_live_smoke_macos.py

<interfaces>
From `omnicontrol/runtime/evidence.py`:
```python
def write_result_bundle(
    profile: str,
    payload: dict[str, Any],
    *,
    report_dir: Path,
    runtime_paths: RuntimePaths | None = None,
) -> dict[str, Any]:
    ...
```

Current artifact normalization boundary:
```python
def _collect_artifacts(payload: dict[str, Any]) -> list[ArtifactRef]:
    for key in CANONICAL_ARTIFACT_KEYS:
        path = _coerce_path(payload.get(key))
```

Current mocked finder-open expectation surface:
```python
payload = run_finder_open_smoke(target_path=None, output_dir=output_dir)
self.assertEqual(payload["report_path"], str(output_dir / "result.json"))
```
</interfaces>
</context>

<read_first>
- `.planning/phases/01-runtime-registry-evidence-foundation/01-UAT.md` for the diagnosed gap statement and severity
- `.planning/phases/01-runtime-registry-evidence-foundation/01-VERIFICATION.md` to preserve the original Phase 1 acceptance intent for `VER-02`
- `.planning/phases/01-runtime-registry-evidence-foundation/01-runtime-registry-evidence-foundation-03-SUMMARY.md` for the existing shared-writer boundary
- `omnicontrol/runtime/evidence.py` to keep the fix inside artifact normalization first
- `tests/test_runtime_evidence.py` and `tests/test_live_smoke_macos.py` to extend the narrowest existing proof points
</read_first>

<source_audit>
| Source Type | Item | Covered By |
|-------------|------|------------|
| GOAL | Evidence-rich smoke bundles with linked artifact paths | Task 1, Task 2 |
| REQ | RTF-04 | Task 2 |
| REQ | VER-02 | Task 1, Task 2 |
| RESEARCH | Canonical shared evidence writer remains the persistence boundary | Task 2 |
| CONTEXT | Keep scope minimal and backward compatible | Task 1, Task 2 |
| UAT GAP | Finder-open generated bundle had empty `artifacts[]` despite real `result.json` output | Task 1, Task 2 |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add a regression for materialized smoke bundles with empty artifacts</name>
  <files>tests/test_runtime_evidence.py, tests/test_live_smoke_macos.py</files>
  <action>Extend the existing Phase 1 evidence tests so they fail against the current behavior described in UAT: a generated `finder-open` bundle can persist `report_path` and runtime metadata while leaving `artifacts[]` empty. Add one direct writer test that covers the fallback case where no existing canonical artifact-bearing payload keys are present, and add one mocked `run_finder_open_smoke()` assertion that reads the persisted `result.json` and proves `artifacts[]` is non-empty. Keep the assertions compatibility-first: do not remove or weaken the current `report_path` expectations, and do not broaden coverage into unrelated smoke profiles.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_runtime_evidence tests.test_live_smoke_macos -v</automated>
  </verify>
  <done>The suite reproduces the UAT gap before the code change and proves the expected fallback artifact behavior after the fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize a concrete fallback artifact for generated smoke bundles</name>
  <files>omnicontrol/runtime/evidence.py, tests/test_runtime_evidence.py, tests/test_live_smoke_macos.py</files>
  <behavior>
    - Test 1: when `write_result_bundle()` materializes `result.json` for a payload with no canonical artifact-bearing keys, persisted `artifacts[]` still contains at least one concrete path tied to the generated bundle.
    - Test 2: mocked `finder-open` smoke persists that fallback artifact reference while preserving the existing top-level `report_path`.
    - Test 3: existing canonical artifact-key normalization still works and is not replaced by the fallback path when real output artifacts already exist.
  </behavior>
  <action>Adjust artifact normalization in `omnicontrol/runtime/evidence.py` so the shared writer emits a concrete fallback artifact reference whenever a smoke bundle was materially generated on disk but payload-driven normalization produced no artifacts. Define that fallback explicitly as bundle/report evidence only, not as synthesized app output: use a distinct artifact label such as `result_bundle` or `report_json`, and a distinct `kind` value that identifies report/bundle evidence semantics. Keep the fix backward compatible: preserve `report_path`, preserve current canonical key handling, and add the fallback only when needed instead of rewriting all artifact semantics. The fallback path must point to the generated bundle/report anchor itself, and canonical payload-derived artifacts must continue to win unchanged when they exist.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_runtime_evidence tests.test_live_smoke_macos tests.test_live_smoke_helpers -v</automated>
  </verify>
  <done>Generated finder-open smoke bundles no longer serialize with empty `artifacts[]`, the fallback entry is explicitly labeled as bundle/report evidence, existing payloads with real output artifacts still serialize cleanly, and the compatibility contract remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime payload → persisted evidence bundle | The writer turns transient runtime output into durable user-facing evidence and must not silently drop all artifact references. |
| Persisted bundle → downstream KB/UAT consumers | Existing callers rely on `report_path`, so the gap fix cannot break the compatibility surface while restoring artifact linkage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | T | `omnicontrol/runtime/evidence.py` | mitigate | Add a regression and fallback normalization so materialized bundles cannot be persisted with empty `artifacts[]` when no profile-specific artifact key exists. |
| T-01-11 | R | `tests/test_live_smoke_macos.py` | mitigate | Assert against the persisted `result.json` for `finder-open`, not only the returned payload, so the practical acceptance path is exercised. |
| T-01-12 | D | `omnicontrol/runtime/evidence.py` | mitigate | If fallback evidence duplicates `report_path`, require explicit bundle/report labeling so downstream consumers can distinguish it from canonical app-output artifacts. |
</threat_model>

<verification>
Run `.venv/bin/python -m unittest tests.test_runtime_evidence tests.test_live_smoke_macos tests.test_live_smoke_helpers -v`.
</verification>

<acceptance_criteria>
- A mocked `finder-open` smoke run writes `result.json` whose `artifacts[]` is non-empty.
- At least one persisted artifact entry contains a concrete on-disk path tied to the generated bundle/report anchor.
- The fallback artifact uses a distinct bundle/report evidence label (`name`, and ideally `kind`) rather than reusing canonical payload artifact names such as `output`, `screenshot`, or `before`.
- Existing top-level `report_path` remains present and unchanged for legacy consumers.
- When canonical payload-derived artifacts already exist, the fallback does not replace them or rename them.
- No unrelated Phase 1 runtime-path or registry behavior is modified.
</acceptance_criteria>

<success_criteria>
The single UAT evidence gap is closed with one targeted writer-layer change and narrow regression coverage, restoring practical `VER-02` acceptance for smoke bundles without reopening broader Phase 1 scope or weakening artifact semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-registry-evidence-foundation/01-runtime-registry-evidence-foundation-04-SUMMARY.md`
</output>
