---
phase: 01-runtime-registry-evidence-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - omnicontrol/cli.py
  - omnicontrol/runtime/registry.py
  - omnicontrol/runtime/paths.py
  - omnicontrol/runtime/kb.py
  - omnicontrol/runtime/live_smoke.py
  - tests/test_runtime_registry.py
  - tests/test_runtime_paths.py
  - tests/test_kb.py
  - tests/test_full_e2e.py
autonomous: true
requirements:
  - RTF-01
  - RTF-02
must_haves:
  truths:
    - "The `smoke` command accepts the same profile IDs, but those IDs now come from one typed registry."
    - "Knowledge and runtime directories resolve to a stable framework-managed root even when OmniControl is launched outside the repo root."
    - "Existing metadata-driven KB behavior still works after the registry extraction."
  artifacts:
    - path: "omnicontrol/runtime/registry.py"
      provides: "Implemented profile registry plus compatibility views for metadata-driven modules"
      exports: ["PROFILE_REGISTRY", "profile_choices", "metadata_for_profile"]
    - path: "omnicontrol/runtime/paths.py"
      provides: "Implemented runtime root resolution and stable KB/run path allocation"
      exports: ["RuntimePaths", "resolve_runtime_paths", "resolve_run_output_dir"]
    - path: "omnicontrol/runtime/kb.py"
      provides: "KB persistence migrated to stable paths with compatibility preserved"
    - path: "omnicontrol/cli.py"
      provides: "Smoke parser choices derived from registry"
  key_links:
    - from: "omnicontrol/cli.py"
      to: "omnicontrol/runtime/registry.py"
      via: "smoke argument choices"
      pattern: "profile_choices|list_profile_ids"
    - from: "omnicontrol/runtime/kb.py"
      to: "omnicontrol/runtime/paths.py"
      via: "stable kb path resolution"
      pattern: "resolve_runtime_paths|kb_path"
    - from: "omnicontrol/runtime/live_smoke.py"
      to: "omnicontrol/runtime/registry.py"
      via: "profile descriptor lookup"
      pattern: "get_profile_descriptor|PROFILE_REGISTRY"
---

<objective>
Implement the typed registry and stable runtime root policy without breaking current smoke commands.

Purpose: remove the two Phase 1 brownfield hazards called out by the research: duplicated profile registration and durable state tied to the invocation directory.
Output: registry-backed CLI/runtime lookup, stable KB/run path resolution, and compatibility adapters that keep existing profile IDs and metadata-driven helpers working.
</objective>

<execution_context>
@/Users/daizhaorong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/daizhaorong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-01-PLAN.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-RESEARCH.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@omnicontrol/cli.py
@omnicontrol/runtime/kb.py
@omnicontrol/runtime/live_smoke.py
@tests/test_kb.py
@tests/test_full_e2e.py

<interfaces>
From the new Phase 1 contract modules created in Plan 01:
```python
@dataclass(frozen=True, slots=True)
class ProfileDescriptor: ...

def list_profile_ids() -> tuple[str, ...]: ...
def profile_choices() -> tuple[str, ...]: ...
def metadata_for_profile(profile: str) -> Mapping[str, Any]: ...
```

```python
@dataclass(slots=True)
class RuntimePaths: ...

def resolve_runtime_paths(...) -> RuntimePaths: ...
def resolve_run_output_dir(...) -> Path: ...
```

Existing callers that must be migrated:
```python
smoke.add_argument("profile", choices=[...])

def kb_path() -> Path:
    return Path.cwd() / "knowledge" / "kb.json"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace duplicated smoke profile registration with one typed registry</name>
  <files>omnicontrol/runtime/registry.py, omnicontrol/cli.py, omnicontrol/runtime/kb.py, omnicontrol/runtime/live_smoke.py, tests/test_runtime_registry.py, tests/test_kb.py, tests/test_full_e2e.py</files>
  <behavior>
    - Test 1: the `smoke` parser accepts the same profile IDs as before, but the choices come from registry helpers rather than an inline literal list.
    - Test 2: KB metadata accessors and secondary-profile inference still receive the same profile IDs and metadata fields after extraction.
    - Test 3: `run_smoke()` still dispatches existing profile IDs and raises `ValueError` for unknown IDs.
  </behavior>
  <action>Implement `registry.py` as the single source of truth for smoke profiles and add compatibility helper views for the metadata currently consumed by `kb.py`, `pivots.py`, and `remediation.py`. Migrate `cli.py` to derive smoke `choices` from the registry. Replace direct `PROFILE_METADATA` ownership with registry-backed compatibility exports or thin wrappers so existing inference logic keeps working. Update `run_smoke()` to validate and dispatch via registry descriptors or registry-owned handler mapping rather than a free-floating if/elif list where feasible, but keep the public function name, profile IDs, and existing per-profile handlers intact. Do not rename profiles, do not change existing command flags, and do not delete compatibility fields that current tests or KB learning depend on.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_runtime_registry tests.test_kb tests.test_full_e2e tests.test_pivots tests.test_remediation tests.test_live_smoke_helpers -v</automated>
  </verify>
  <done>All existing smoke profile IDs are registry-derived, CLI and runtime use the same source of truth, and KB metadata consumers still work without profile-id churn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move durable runtime state to stable framework-managed roots</name>
  <files>omnicontrol/runtime/paths.py, omnicontrol/runtime/kb.py, omnicontrol/runtime/live_smoke.py, tests/test_runtime_paths.py, tests/test_kb.py</files>
  <behavior>
    - Test 1: `kb_path()` resolves to a stable runtime root independent of `cwd`.
    - Test 2: explicit per-run output overrides still take precedence for caller-owned artifact destinations.
    - Test 3: legacy repo-root knowledge can still be read or migrated so existing local history is not silently lost.
  </behavior>
  <action>Implement the runtime root policy in `paths.py` and migrate `kb.py` plus the runtime output-directory helpers in `live_smoke.py` to use it. Use an additive precedence model: explicit output argument wins for caller-owned artifacts; otherwise resolve durable runtime state from one stable framework-managed root; if a legacy repo-root `knowledge/kb.json` exists, preserve it through compatibility read/migration rather than abandoning it. Replace direct JSON writes in `save_kb()` with staged temp-file plus atomic replace to close the torn-write hole identified in concerns. Keep the migration narrow to Phase 1 scope: do not redesign benchmark/scaffold output defaults here unless they are required by shared helpers.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_runtime_paths tests.test_kb tests.test_full_e2e tests.test_pivots tests.test_remediation tests.test_live_smoke_helpers -v</automated>
  </verify>
  <done>KB and default smoke runtime roots are no longer tied to the invocation directory, explicit overrides still work, and current local knowledge is preserved through the migration path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/profile input → registry lookup | User-provided profile IDs must map through validated descriptors, not scattered literals. |
| Runtime → persistent knowledge store | Durable state crosses into a shared filesystem location and must be written atomically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `omnicontrol/runtime/kb.py` | mitigate | Replace direct `write_text()` persistence with temp-file plus atomic replace for `kb.json`. |
| T-01-05 | T | `omnicontrol/runtime/paths.py` | mitigate | Centralize runtime root precedence so durable state cannot drift with shell `cwd`. |
| T-01-06 | D | `omnicontrol/cli.py` + `omnicontrol/runtime/live_smoke.py` | mitigate | Preserve public profile IDs and dispatch behavior through registry-backed tests before and after migration. |
</threat_model>

<verification>
Run `.venv/bin/python -m unittest tests.test_runtime_registry tests.test_runtime_paths tests.test_kb tests.test_full_e2e tests.test_pivots tests.test_remediation tests.test_live_smoke_helpers -v`, then run `.venv/bin/python -m unittest tests.test_live_smoke_macos -v` to confirm the runtime path migration did not break mocked macOS smoke entrypoints.
</verification>

<success_criteria>
The smoke registry is typed and singular, durable KB/runtime roots are stable outside the repo root, legacy profile IDs still work unchanged, and KB persistence no longer depends on non-atomic direct writes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-registry-evidence-foundation/01-runtime-registry-evidence-foundation-02-SUMMARY.md`
</output>
