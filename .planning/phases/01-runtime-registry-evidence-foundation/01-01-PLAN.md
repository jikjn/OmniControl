---
phase: 01-runtime-registry-evidence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - omnicontrol/runtime/registry.py
  - omnicontrol/runtime/paths.py
  - omnicontrol/runtime/evidence.py
  - tests/test_runtime_registry.py
  - tests/test_runtime_paths.py
  - tests/test_runtime_evidence.py
  - tests/test_kb.py
  - tests/test_full_e2e.py
  - tests/test_live_smoke_helpers.py
autonomous: true
requirements:
  - RTF-01
  - RTF-02
  - RTF-04
  - VER-01
  - VER-02
must_haves:
  truths:
    - "The runtime has one typed source of truth for smoke profile IDs and their metadata."
    - "Stable runtime and evidence paths are defined independently of the caller's working directory."
    - "A canonical result bundle contract exists before runtime migration begins."
  artifacts:
    - path: "omnicontrol/runtime/registry.py"
      provides: "Typed profile descriptor and registry query helpers"
      exports: ["ProfileDescriptor", "PROFILE_REGISTRY", "list_profile_ids"]
    - path: "omnicontrol/runtime/paths.py"
      provides: "Stable runtime root policy and resolved path dataclasses"
      exports: ["RuntimePaths", "resolve_runtime_paths"]
    - path: "omnicontrol/runtime/evidence.py"
      provides: "Result bundle and artifact reference contract"
      exports: ["ArtifactRef", "ResultBundle", "write_result_bundle"]
    - path: "tests/test_runtime_registry.py"
      provides: "Regression coverage for registry-derived CLI choices and compatibility views"
    - path: "tests/test_runtime_paths.py"
      provides: "Regression coverage for cwd-independent path resolution"
    - path: "tests/test_runtime_evidence.py"
      provides: "Regression coverage for canonical result bundle shape"
  key_links:
    - from: "tests/test_runtime_registry.py"
      to: "omnicontrol/runtime/registry.py"
      via: "registry import and profile-id assertions"
      pattern: "from omnicontrol\\.runtime\\.registry import"
    - from: "tests/test_runtime_paths.py"
      to: "omnicontrol/runtime/paths.py"
      via: "runtime root precedence assertions"
      pattern: "resolve_runtime_paths|RuntimePaths"
    - from: "tests/test_runtime_evidence.py"
      to: "omnicontrol/runtime/evidence.py"
      via: "bundle serialization assertions"
      pattern: "write_result_bundle|ResultBundle"
---

<objective>
Freeze the Phase 1 migration contracts before behavior changes land.

Purpose: make the brownfield extraction safe by defining the registry, runtime-path, and evidence interfaces up front and by encoding the required compatibility behavior in tests.
Output: new unittest coverage plus interface-first skeleton modules that downstream plans implement against.
</objective>

<execution_context>
@/Users/daizhaorong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/daizhaorong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-RESEARCH.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@omnicontrol/cli.py
@omnicontrol/runtime/contracts.py
@omnicontrol/runtime/kb.py
@omnicontrol/runtime/live_smoke.py
@tests/test_kb.py
@tests/test_live_smoke_macos.py
@tests/test_full_e2e.py

<interfaces>
From `omnicontrol/cli.py`:
```python
smoke = subparsers.add_parser("smoke")
smoke.add_argument("profile", choices=[...])
payload = run_smoke(
    args.profile,
    source=args.source,
    output=args.output,
    query=args.query,
    url=args.url,
    chrome_path=args.chrome_path,
    word_path=args.word_path,
)
```

From `omnicontrol/runtime/kb.py`:
```python
PROFILE_METADATA: dict[str, dict[str, Any]] = {...}

def kb_path() -> Path:
    return Path.cwd() / "knowledge" / "kb.json"

def record_payload(profile: str, payload: dict[str, Any]) -> dict[str, Any]:
    ...
```

From `omnicontrol/runtime/live_smoke.py`:
```python
def run_smoke(profile: str, *, source: str | None = None, output: str | None = None, ...) -> dict:
    ...

def _finalize_payload(profile: str, payload: dict[str, Any]) -> dict[str, Any]:
    ...
```

From `omnicontrol/runtime/contracts.py`:
```python
SMOKE_CONTRACTS: dict[str, SmokeContract] = {...}
```
</interfaces>
</context>

<source_audit>
| Source Type | Item | Covered By |
|-------------|------|------------|
| GOAL | Typed registry as one source of truth | Plan 01 task 2, Plan 02 task 1 |
| GOAL | Stable runtime state independent of cwd | Plan 01 task 1, Plan 02 task 2 |
| GOAL | Evidence-rich `result.json` artifacts | Plan 01 task 1, Plan 03 tasks 1-2 |
| REQ | RTF-01 | Plan 01 task 1, Plan 02 task 1 |
| REQ | RTF-02 | Plan 01 task 1, Plan 02 task 2 |
| REQ | RTF-04 | Plan 01 task 1, Plan 03 task 1 |
| REQ | VER-01 | Plan 01 task 1, Plan 03 tasks 1-2 |
| REQ | VER-02 | Plan 01 task 1, Plan 03 tasks 1-2 |
| RESEARCH | Thin typed registry with compatibility wrappers | Plan 01 task 2, Plan 02 task 1 |
| RESEARCH | `RuntimePaths` helper replacing durable `Path.cwd()` usage | Plan 01 task 2, Plan 02 task 2 |
| RESEARCH | Canonical `write_result_bundle()` replacing ad hoc `result.json` writes | Plan 01 task 2, Plan 03 task 1 |
| CONTEXT | Brownfield-safe additive migration preserving existing behavior | Plan 01 task 1, Plan 02 tasks 1-2, Plan 03 task 2 |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Codify the Phase 1 behavior as unittest regressions</name>
  <files>tests/test_runtime_registry.py, tests/test_runtime_paths.py, tests/test_runtime_evidence.py, tests/test_kb.py, tests/test_full_e2e.py</files>
  <action>Create new `unittest.TestCase` modules following existing suite patterns to lock the required migration behavior before implementation. Cover registry-derived smoke profile choices and preserved profile IDs for `RTF-01`; cwd-independent runtime root and knowledge-base resolution for `RTF-02`; canonical `result.json` bundle shape plus concrete `artifacts[]` references for `RTF-04`, `VER-01`, and `VER-02`. Extend existing KB and CLI tests rather than replacing them, including a mocked `omnicontrol smoke ... --json` CLI regression that proves the registry-backed user entrypoint still parses, dispatches, and returns structured output. Preserve current public profile IDs verbatim and assert that legacy fields such as `report_path` remain available during the migration, because downstream KB learning already consumes them.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_runtime_registry tests.test_runtime_paths tests.test_runtime_evidence tests.test_kb tests.test_full_e2e tests.test_live_smoke_helpers -v</automated>
  </verify>
  <done>Tests fail for missing Phase 1 behavior, include the mocked `smoke` CLI path, and clearly express the required compatibility guarantees without depending on real macOS automation.</done>
</task>

<task type="auto">
  <name>Task 2: Add interface-first runtime contracts for registry, paths, and evidence</name>
  <files>omnicontrol/runtime/registry.py, omnicontrol/runtime/paths.py, omnicontrol/runtime/evidence.py</files>
  <action>Create import-safe skeleton modules with slotted/frozen dataclasses and exported helper signatures only. `registry.py` should declare a typed `ProfileDescriptor` model plus read-only query helpers that later plans will use to derive CLI choices, dispatch, and KB compatibility views. `paths.py` should declare the stable root policy, resolved runtime path dataclasses, and explicit override parameters. `evidence.py` should declare typed artifact references and the canonical `write_result_bundle()` boundary. Keep these modules additive-first: no callers are switched yet, no profile logic is moved yet, and no existing behavior is deleted.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_runtime_registry tests.test_runtime_paths tests.test_runtime_evidence -v</automated>
  </verify>
  <done>Downstream plans can implement against exported contracts instead of rediscovering requirements in `cli.py`, `kb.py`, and `live_smoke.py`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input → runtime registry | Untrusted profile IDs and overrides must resolve through typed descriptors instead of scattered literals. |
| Runtime process → filesystem | Durable state and result artifacts cross into persistent storage and must not depend on attacker-controlled `cwd`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `omnicontrol/runtime/paths.py` | mitigate | Encode tests for stable root precedence and forbid durable path derivation from `Path.cwd()` except explicit caller-owned outputs. |
| T-01-02 | T | `omnicontrol/runtime/evidence.py` | mitigate | Define canonical bundle fields and artifact references in tests before migration so later implementations cannot silently drop evidence links. |
| T-01-03 | D | `omnicontrol/runtime/registry.py` | mitigate | Preserve existing profile IDs in regression tests so CLI dispatch cannot break due to registry extraction. |
</threat_model>

<verification>
Run `.venv/bin/python -m unittest tests.test_runtime_registry tests.test_runtime_paths tests.test_runtime_evidence tests.test_kb tests.test_full_e2e tests.test_live_smoke_helpers -v` after the test and skeleton modules are added.
</verification>

<success_criteria>
Phase 1 migration contracts are executable: failing tests exist for every in-scope requirement, interface modules exist with exported contracts, and no current CLI/profile behavior has been removed yet.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-registry-evidence-foundation/01-runtime-registry-evidence-foundation-01-SUMMARY.md`
</output>
