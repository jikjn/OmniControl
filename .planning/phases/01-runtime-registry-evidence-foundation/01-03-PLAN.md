---
phase: 01-runtime-registry-evidence-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - omnicontrol/runtime/evidence.py
  - omnicontrol/runtime/live_smoke.py
  - omnicontrol/runtime/kb.py
  - omnicontrol/runtime/contracts.py
  - tests/test_runtime_evidence.py
  - tests/test_live_smoke_macos.py
  - tests/test_kb.py
autonomous: true
requirements:
  - RTF-04
  - VER-01
  - VER-02
must_haves:
  truths:
    - "Every Phase 1 macOS smoke run emits one canonical `result.json` bundle."
    - "The bundle references concrete artifact paths instead of only status text."
    - "Existing KB learning and CLI output still see `report_path` and relevant legacy evidence keys during the migration."
  artifacts:
    - path: "omnicontrol/runtime/evidence.py"
      provides: "Implemented canonical result bundle writer and artifact reference normalization"
      exports: ["ArtifactRef", "ResultBundle", "write_result_bundle"]
    - path: "omnicontrol/runtime/live_smoke.py"
      provides: "Runtime wired to the shared evidence writer"
    - path: "tests/test_runtime_evidence.py"
      provides: "Regression coverage for bundle fields and artifact references"
  key_links:
    - from: "omnicontrol/runtime/live_smoke.py"
      to: "omnicontrol/runtime/evidence.py"
      via: "shared result writer"
      pattern: "write_result_bundle"
    - from: "omnicontrol/runtime/kb.py"
      to: "omnicontrol/runtime/evidence.py"
      via: "report path and artifact reference reuse"
      pattern: "artifacts|report_path"
    - from: "tests/test_live_smoke_macos.py"
      to: "omnicontrol/runtime/live_smoke.py"
      via: "mocked macOS smoke artifact assertions"
      pattern: "result\\.json|artifacts"
---

<objective>
Centralize result bundle writing so every run produces one structured report with linked evidence artifacts.

Purpose: finish Phase 1 by replacing repeated ad hoc `result.json` writes with one writer that later phases can extend without another report-format break.
Output: canonical bundle writer, runtime integration across smoke profiles, and compatibility-preserving evidence references for KB learning and CLI consumers.
</objective>

<execution_context>
@/Users/daizhaorong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/daizhaorong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-02-PLAN.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-RESEARCH.md
@.planning/phases/01-runtime-registry-evidence-foundation/01-PATTERNS.md
@omnicontrol/runtime/contracts.py
@omnicontrol/runtime/evidence.py
@omnicontrol/runtime/kb.py
@omnicontrol/runtime/live_smoke.py
@tests/test_runtime_evidence.py
@tests/test_live_smoke_macos.py

<interfaces>
Existing normalization boundary:
```python
def _finalize_payload(profile: str, payload: dict[str, Any]) -> dict[str, Any]:
    contract = SMOKE_CONTRACTS.get(profile)
    ...
    payload["strategy"] = structured.to_dict()
    return payload
```

Current repeated persistence pattern to replace:
```python
report_path = output_dir / "result.json"
report_path.write_text(json.dumps(payload, indent=2, ensure_ascii=False), encoding="utf-8")
payload["report_path"] = str(report_path)
payload = _record_payload(profile, payload)
```

Contract dependency that must remain compatible:
```python
case["blocked_cases"].append(
    {
        "evidence": payload.get("strategy", {}).get("evidence", {}),
        "report_path": payload.get("report_path"),
    }
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the canonical result bundle and artifact reference writer</name>
  <files>omnicontrol/runtime/evidence.py, omnicontrol/runtime/contracts.py, tests/test_runtime_evidence.py</files>
  <behavior>
    - Test 1: `write_result_bundle()` emits `result.json` with run identity, profile identity, status, timestamps, runtime-root metadata, and `artifacts[]`.
    - Test 2: artifact references contain concrete paths for known outputs such as `output`, `screenshot`, `before`, `after`, or profile-specific files when present.
    - Test 3: compatibility fields such as `report_path` and profile-specific evidence keys remain available to existing callers during the migration.
  </behavior>
  <action>Implement `evidence.py` so it transforms a finalized runtime payload plus resolved runtime paths into one canonical bundle writer. Normalize concrete artifact references into a typed `artifacts[]` collection while preserving existing top-level payload keys needed by KB learning and current CLI presentation. Keep the Phase 1 contract intentionally narrow: capture run/profile identity, status, timestamps, strategy summary, runtime path metadata, and artifact references now; leave richer blocker taxonomy expansion to Phase 2. Update `contracts.py` only if a small declarative mapping is needed to identify canonical artifact-bearing fields; do not push profile-specific branching back into `live_smoke.py`.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_runtime_evidence -v</automated>
  </verify>
  <done>The result writer is reusable, bundle fields are standardized, artifact references are explicit, and legacy payload consumers remain compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate smoke runtime report emission to the shared evidence pipeline</name>
  <files>omnicontrol/runtime/live_smoke.py, omnicontrol/runtime/kb.py, tests/test_live_smoke_macos.py, tests/test_kb.py, tests/test_runtime_evidence.py</files>
  <behavior>
    - Test 1: mocked macOS smoke helpers still return `report_path`, but the underlying `result.json` now follows the canonical bundle shape.
    - Test 2: KB persistence still records `report_path` and can also consume canonical artifact references where useful.
    - Test 3: no per-profile `result.json` write sites remain in the migrated Phase 1 runtime paths.
  </behavior>
  <action>Replace repeated `report_path.write_text(...)` blocks in `live_smoke.py` with one shared writer call that runs after `_finalize_payload()` and before `_record_payload()`. Migrate the Phase 1-relevant macOS and shared runtime paths first, and only wrap additional direct write sites that materially affect the shared evidence contract in this phase instead of attempting a repo-wide rewrite. Update `kb.py` only as needed to preserve `report_path` compatibility and optionally store canonical artifact metadata alongside existing `strategy.evidence` without changing the public KB shape unnecessarily. Keep the migration additive-first and behavior-preserving: profile handlers keep their current return payload keys and current tests should continue to pass with only strengthened artifact assertions added.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_runtime_evidence tests.test_live_smoke_macos tests.test_live_smoke_helpers tests.test_kb tests.test_pivots tests.test_remediation -v</automated>
  </verify>
  <done>All Phase 1 smoke runs emit the canonical bundle through one writer, artifact references are concrete, and existing CLI/KB consumers still work without interface breaks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime payload → persisted result bundle | Execution output becomes a durable evidence artifact consumed by users and KB learning. |
| Result bundle → knowledge base | Evidence references are reused by the KB, so compatibility fields cannot disappear unexpectedly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | `omnicontrol/runtime/evidence.py` | mitigate | Use one writer boundary so artifact references and `result.json` shape are produced consistently for every run. |
| T-01-08 | R | `omnicontrol/runtime/live_smoke.py` | mitigate | Include run/profile identity, timestamps, and canonical `report_path` in every persisted bundle to preserve auditability. |
| T-01-09 | D | `omnicontrol/runtime/kb.py` | accept | Existing KB consumers remain local-only in this phase; preserve `report_path` and evidence compatibility to avoid self-inflicted data loss. |
</threat_model>

<verification>
Run `.venv/bin/python -m unittest tests.test_runtime_evidence tests.test_live_smoke_macos tests.test_live_smoke_helpers tests.test_kb tests.test_pivots tests.test_remediation -v`, then run `.venv/bin/python -m unittest discover -s tests -v` as the phase gate.
</verification>

<success_criteria>
There is one canonical result writer, `result.json` includes explicit artifact references, mocked macOS smoke runs still pass, and no Phase 1 runtime path writes ad hoc reports outside the shared evidence boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-registry-evidence-foundation/01-runtime-registry-evidence-foundation-03-SUMMARY.md`
</output>
